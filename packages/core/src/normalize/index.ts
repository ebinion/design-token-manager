// Loose -> canonical $type map (text->fontFamily, number->dimension/fontWeight).
// Read/display-only: never written back unless the user explicitly remaps
// (FR-1.2a, PRD §6 design note). A table, not per-type code.
export {};
