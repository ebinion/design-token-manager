// Unified reference resolver: {alias} + $ref (JSON-Pointer) + $extends modeled
// as edges in one dependency graph, with a single cycle check
// (FR-1.3 / FR-1.4 / AC-5). Build, don't borrow (PRD §6 design note).
export {};
