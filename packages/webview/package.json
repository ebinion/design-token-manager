{
  "name": "@dtm/webview",
  "version": "0.0.1",
  "description": "Vanilla TS + CSS GUI rendered inside the Custom Text Editor webview.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.mjs --production",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
